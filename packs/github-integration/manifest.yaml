name: github-integration
version: 0.7.2
depends_on: [core]

install:
  copy:
    - from: scaffold/root/.github/
      to: .github/
  merge: []

verify:
  must_exist:
    - .github/workflows/branch-prune.yml
